import React from 'react'

const LogoPart = ({className}) => {
  return (
    <div>
      <span className={`${className} text-2xl lg:text-3xl  font-bold bg-gradient-to-r  from-[rgb(135,80,247)] to-white bg-clip-text text-transparent`}> Asaduzzaman Rahim </span>
    </div>
  )
}

export default LogoPart
